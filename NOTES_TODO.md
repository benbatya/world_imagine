How to run:
    

Notes for future changes:
1. Move gsplat ordering into a compute shader instead of doing it on the CPU
2. Implement a Fly camera to complement the Orbit Camera
3. Make the rendering incremental so that the outline of the scene is displayed while it is being loaded

