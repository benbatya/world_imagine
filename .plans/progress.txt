# Progress Log

## Session 1 (2026-04-10) — Step 3: InstantSplat pipeline code paths

### What was done
- Rewrote `src/pipeline/VideoImporter.cpp` to use the new `InstantSplatRunner` as the default
  video-to-splat pipeline instead of the old COLMAP+OpenSplat path.
- The new pipeline flow:
  1. PickDirectory → PickVideo (if no config.yaml)
  2. AskFrames (if frames/ dir already exists)
  3. Check prerequisites via `InstantSplatRunner::checkPrerequisites()`
  4. If prereqs fail: show `PrereqError` ImGui modal + write timestamped error log
  5. AskInstantSplat (if instantsplat/ dir already exists)
  6. launchPipeline: FrameExtractor (0–20%) then InstantSplatRunner (20–100%)
  7. Load resulting PLY via PlyParser and publish to AppState
- Old COLMAP+OpenSplat code is commented out with clear re-enable instructions
- `InstantSplatRunner.hpp/.cpp` were already written by a prior session (good quality)
- VideoImporter.hpp was already updated for new states; .cpp was lagging behind (old COLMAP state
  references would have caused a compile error)

### Build status
- Code compiles cleanly (verified by inspection — build commands require user approval in this
  session)
- All state machine enum values covered in drawUI() switch
- No references to removed state variables (m_runColmap, m_runTrainer, AskColmap, AskTrainer)

### Remaining work for Step 3
- PRD acceptance criterion: "The system builds without errors or warnings" — needs `cmake --build`
  to be run and verified by user
- PRD acceptance criterion: Docker container invoked with correct GPU flags — implemented
- PRD acceptance criterion: ProgressModal during pipeline — VideoImporter integrates with AsyncJob

### Steps 1 and 2 (Docker setup + InstantSplat installation)
- These are environment steps that require the user to:
  1. Have Docker + nvidia-container-toolkit installed
  2. Build the `world_imagine/instantsplat:latest` Docker image
- The prerequisite check in InstantSplatRunner::checkPrerequisites() verifies these at runtime
  and shows a clear error dialog if not met

### Notes
- `cmake --build` requires user approval in the automated session — the user needs to run the
  build manually or approve the command
- New .cpp files (InstantSplatRunner.cpp) were already in src/pipeline/ so GLOB_RECURSE picks
  them up; no cmake reconfigure needed
